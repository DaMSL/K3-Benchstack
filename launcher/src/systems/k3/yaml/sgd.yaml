---
volumes:
  - {host: /local, container: /local}
name: role 1
peers: 128
hostmask: ".*qp-hm.*"
k3_globals:
  role: points
  master: auto
  dimensionality: 32
  iterations_remaining: 10
k3_data:
  - {var: dataFiles, path: /local/data/sgd, policy: global}
